# Current Exercise State

Update cadence: review and update every Friday before planning the next content push.

- Weekly content planning is stalled because `inbox.md` has already been mined and needs fresh raw capture before new queue items can pass the dedup gate.
- Publishing reconciliation is complete because all 17 current queue items are filed as `published` with no scheduled or ready-for-publishing posts remaining.
- Fresh capture is the next active exercise because the next Road4AI content batch depends on new notes about what broke, what shipped, and what is waiting on the next free request window.
- GitNexus refresh is batched because `npx gitnexus analyze` should run at end-of-day or after several commits, not after every token-anxiety log entry.
